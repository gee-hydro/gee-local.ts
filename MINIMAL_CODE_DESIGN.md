# 极简代码设计

极简不是代码最短，而是以最少概念清楚表达问题，使代码易读、易改、易验证。

## 原则

1. **数据流优先**：先明确输入、变换和输出，再划分模块；主流程保持线性、显式。
2. **职责单一**：一个函数只完成一个动作；删除仅转发参数或包装变量的中间层。
3. **分离变化**：业务规则留在业务层，通用机制集中复用；用小型适配器隔离差异。
4. **依赖显式**：函数只接收真正需要的参数，不依赖隐式全局状态或万能配置对象。
5. **克制抽象**：仅抽取含义稳定、可独立测试且确实复用的逻辑；少量重复优于错误抽象。
6. **副作用收口**：I/O、状态修改和外部调用集中在边界，核心逻辑尽量纯净。
7. **约束明确**：类型表达结构，校验保护危险边界，默认值放在使用处；关键业务参数不得隐藏。
8. **验证可追溯**：注释解释原因与约束，不复述代码；输入、输出和验证结果应可复现、可对应。

## 判断标准

读者应能从上到下一遍看懂：数据从哪里来，经过什么处理，最终到哪里去。若理解代码需要频繁跳转、追踪隐式状态或穿过多层包装，设计仍不够简单。

{
  "name": "gee-helper",
  "version": "0.1.0",
  "description": "GEE authentication, batch export, and local Code Editor runtime",
  "type": "commonjs",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "bin": {
    "ee": "bin/ee"
  },
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "default": "./dist/index.js"
    },
    "./auth": {
      "types": "./dist/auth.d.ts",
      "default": "./dist/auth.js"
    },
    "./export": {
      "types": "./dist/export/batches.d.ts",
      "default": "./dist/export/batches.js"
    }
  },
  "workspaces": [
    "packages/ee-auth"
  ],
  "files": [
    "dist",
    "README.md",
    "bin"
  ],
  "scripts": {
    "build": "npm run build -w ee-auth && tsc",
    "typecheck": "npm run typecheck -w ee-auth && tsc --noEmit",
    "test": "npm run build && tsx --test test/**/*.test.ts packages/ee-auth/test/**/*.test.ts",
    "test:coverage": "npm run build && c8 --reporter=text --reporter=lcov tsx --test test/**/*.test.ts packages/ee-auth/test/**/*.test.ts",
    "prepare": "npm run build"
  },
  "c8": {
    "include": [
      "src/**/*",
      "packages/ee-auth/src/**/*"
    ],
    "exclude": [
      "src/**/*.d.ts",
      "test/**"
    ],
    "reporter": [
      "text",
      "lcov"
    ],
    "report-dir": "coverage",
    "all": true
  },
  "dependencies": {
    "ee-auth": "*"
  },
  "devDependencies": {
    "@types/node": "^20.12.0",
    "c8": "^12.0.0",
    "tsx": "^4.19.0",
    "typescript": "^5.4.5"
  }
}
